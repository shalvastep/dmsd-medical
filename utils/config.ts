import { IConfig } from 'utils/config.interface';

const config: IConfig = {
	serverApiPath: 'medical-service',
	serverHost: 'http://localhost:8080',
	host: 'http://localhost:3000'
};

export default config;
